#include <stdio.h>

/**
* main - prints numbers from 0 to 9.
*
*return: always 0 (success)
*/
int main(void)
{
	int i;
	for (i = o; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}

