#include <stdio.h>

/**
* main - prints the lowercase alphabet in reverse,
* followed by a new line
* return: always 0 (success)
*/
int main (void)
{
	char ch;
	
	for (ch = 'z'; ch >= 'a'; ch--)
	{
		putchar('\n');
	putchar('\n');
	return (0);
}
